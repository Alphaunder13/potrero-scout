# Potrero Scout — dependencias V2, PINNEADAS (el SPEC lo pide y el deploy lo
# necesita: versiones exactas = builds reproducibles en Streamlit Cloud).
# Nota: cambiar este archivo fuerza a Cloud a reconstruir el entorno (reinicio
# completo del proceso), a diferencia de un hot-deploy de solo codigo.

# --- Runtime de la app (Streamlit Cloud instala esto) ---
pandas==3.0.3
streamlit==1.58.0
python-dotenv==1.2.2

# --- Capa de IA (CLI local: reports/claude_report.py) ---
anthropic==0.116.0

# --- Ingesta local (no la usa la app deployada; ScraperFC se instala aparte) ---
requests==2.34.2
httpx==0.28.1
beautifulsoup4==4.15.0
lxml==6.1.1

# --- Tests ---
pytest==9.1.1
