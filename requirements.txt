# Potrero Scout — dependencias V1
# NOTA: versiones con minimos flexibles por ahora. Una vez que instale OK,
# las clavamos con `pip freeze > requirements.txt` (el SPEC pide pin-eado).

# --- Datos ---
pandas>=2.2
requests>=2.32
httpx>=0.27
python-dotenv>=1.0        # cargar las API keys desde .env

# --- Capa de IA (etapa posterior) ---
anthropic>=0.40

# --- Dashboard ---
streamlit>=1.40

# --- Scraping Transfermarkt / fallback Sofascore (se activan cuando lleguemos) ---
beautifulsoup4>=4.12
lxml>=5.2
# ScraperFC>=3.0          # fallback Sofascore, descomentar en la etapa de ingesta

# --- Tests ---
pytest>=8.0
