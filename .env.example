# Copiá este archivo a .env y completá con tus claves reales.
# El .env NUNCA se commitea (está en .gitignore).

# API-Football (https://www.api-football.com/) — tier gratis, 100 req/día.
# Necesaria para el Paso 2 (validación de datos de Primera Nacional).
API_FOOTBALL_KEY=

# API de Anthropic (https://console.anthropic.com/) — pago por uso.
# Solo hace falta cuando lleguemos a la capa de IA (informes). Dejala vacía por ahora.
ANTHROPIC_API_KEY=
